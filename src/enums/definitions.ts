export enum PriorityType {
  NONE = 0,
  LOW = 1,
  MEDIUM = 2,
  HIGH = 3,
}

export enum ActivityType {
  UPDATE_PRIORITY_ACTIVITY_TYPE = 2,
}

export enum Status {
  SUCCESS = "success",
  ERROR = "error",
}
